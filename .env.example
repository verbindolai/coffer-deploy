# ===========================================
# Coffer Self-Hosted Deployment Configuration
# ===========================================
# Copy this file to .env and fill in the required values

# -----------------
# Database Settings
# -----------------
# PostgreSQL database name (default: coffer)
DB_NAME=coffer

# PostgreSQL username (default: coffer)
DB_USER=coffer

# PostgreSQL password (REQUIRED - no default for security)
DB_PASSWORD=changeme_use_a_strong_password

# -----------------
# External APIs
# -----------------
# Numista API key for coin catalog lookups (optional, but recommended)
# Get your API key at: https://en.numista.com/api/
NUMISTA_API_KEY=your_numista_api_key_here

# -----------------
# Application Settings
# -----------------
# Port to expose the frontend (default: 80)
FRONTEND_PORT=80

# JVM options for the backend (default: -Xmx512m)
# Increase memory for larger collections
JAVA_OPTS=-Xmx512m
